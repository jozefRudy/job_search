// TODO: implement fake embedding module
// - model_id set on startup (CLI / server); embedder returns (model_id, Vec<f32>)
// - return deterministic Vec<f32> of fixed dimension for each input
// - enough to wire up search/DB plumbing; replace with real model later
